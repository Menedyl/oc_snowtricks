<nav class="navbar navbar-default">

    <div class="collapse navbar-collapse">

        <ul class="nav navbar-nav">

            <li><a href="{{ path("home") }}">Accueil</a></li>

            {% if is_granted('ROLE_USER') %}
                <li><a href="{{ path("add_figure") }}">Ajouter figure</a></li>
            {% endif %}

            <li class="dropdown">

                <a class="dropdown-toggle" data-toggle="dropdown" href="#">Groupe de figure
                    <span class="caret"></span></a>

                <ul class="dropdown-menu">

                    {% for group in groupsFigure %}

                        <li><a href="{{ path("group_figure", {'id' : group.id}) }}">{{ group.name }}</a></li>

                    {% endfor %}

                </ul>

            </li>

            <li><a href="{{ path('lexique') }}">Lexique</a></li>
            <li><a href="{{ path("contact") }}">Contact</a></li>
            <li><a href="{{ path("mentions") }}">Mention légales</a></li>

        </ul>

    </div>
</nav>
